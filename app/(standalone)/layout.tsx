// Layout trắng cho các trang đứng độc lập 
// Không có Navbar, Footer — chỉ render children
export default function StandaloneLayout({ children }: { children: React.ReactNode }) {
    return children
}
